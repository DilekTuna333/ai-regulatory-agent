[project]
name = "weather-chat-amp"
version = "0.1.0"
description = "CrewAI crew that chats and can fetch current weather via Open-Meteo"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
  "crewai",
  "requests",
  "pyyaml"
]

[tool.crewai]
type = "crew"
